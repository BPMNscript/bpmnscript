{
  "name": "tests",
  "description": "Integration and E2E tests for BPMNscript",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "clean": "shx rm -fr *.tsbuildinfo out",
    "typecheck": "tsc -b tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@bpmn-script/language": "0.0.1",
    "@bpmn-script/transform": "0.0.1",
    "langium": "^4.3.1",
    "testcontainers": "^12.0.4",
    "vitest": "^4.1.10",
    "vscode-languageserver-types": "^3.18.0"
  }
}
