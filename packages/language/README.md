# @bpmn-script/language

The Langium language package for BPMNscript. Provides the grammar, parser, AST, validator, and language server used by the VS Code extension and the CLI.

## In plain terms

This package knows the BPMNscript _language_ itself: what counts as valid syntax, and how to turn source text into a structured tree the rest of the toolchain can work with.

You write the language's rules once, in a single grammar file (`src/bpmn-script.langium`). [Langium](https://langium.org/) reads that grammar and generates three things for you: a **parser** (text → tree), the **AST** types (the tree's shape, as TypeScript interfaces), and a **language server** that powers the in-editor experience — syntax highlighting, autocompletion, and the red squiggles under mistakes. None of that generated code is checked in; `npm run build` regenerates it from the grammar.

The only logic written by hand on top of Langium is the **validator** (`src/bpmn-script-validator.ts`): the structural rules a parser alone can't express, such as "a service task needs exactly one binding attribute (`class`, `expression`, or `delegate`)" or "a variable reference must match a declared `var` type." Langium already reports purely syntactic errors and unresolved references; the validator adds the BPMN-specific and type-system rules.

## Purpose

This package contains everything Langium generates and everything written on top of it:

- The BPMNscript grammar (`src/bpmn-script.langium`) covering the supported BPMN subset.
- The structural validator (`src/bpmn-script-validator.ts`) for AST-level BPMN constraints.
- The Langium-generated parser, AST types, and language server module (built artifacts in `out/`; source in `src/generated/` — both git-ignored, regenerated by `npm run build`).
- The base VS Code TextMate grammar (`syntaxes/bpmn-script.tmLanguage.json`, git-ignored, generated by `langium generate`). A separate hand-maintained injection grammar in `packages/extension` layers embedded-language highlighting onto a `script` task's fenced body on top of this base grammar — see [`packages/extension/README.md`](../extension/README.md).

## Public API surface

```ts
import {
  createBpmnScriptServices, // factory: returns a fully wired Langium service container
  BpmnScriptLanguageMetaData, // file-extension constants (e.g. fileExtensions: ['bpmnscript'])
  renderExpression, // serialize a parsed expression AST node to a "${…}" string
} from '@bpmn-script/language';

import type {
  Model, // root AST node (one or more processes)
  Process, // process block
  Statement, // union of all executable statement kinds
  IfStatement,
  ElseIf,
  WhileStatement,
  DoWhileStatement,
  ParallelStatement,
  GotoStatement,
  SubProcess,
  StartEvent,
  EndEvent,
  UserTask,
  ServiceTask,
  ExternalTask,
  ScriptTask,
  Attribute,
  VarDecl,
  Expr,
  VarRef, // expression AST nodes
} from '@bpmn-script/language';
```

The generated AST types (`Model`, `Process`, `Statement`, `IfStatement`, `WhileStatement`, `ParallelStatement`, …) are also re-exported from the package root and consumed by `@bpmn-script/transform`'s `astToIr` function.

## Grammar overview

The grammar accepts files with one `process` block per file. Sequence flow is implicit (top-to-bottom); control flow is expressed with structured statements. Optional `var` declarations go in the process header before the body.

```bpmnscript
process invoice-approval {
  var amount: number

  start ReviewStart
  user ReviewInvoice { assignee = "demo" }
  if (amount > 1000) {
    user SeniorApproval { assignee = "manager" }
  } else {
    service AutoApprove { class = "com.example.AutoApproveDelegate" }
  }
  end Done
}
```

Supported statements: `start`, `end`, `user` (attribute block: `assignee`, `formKey`), `service` (attribute block: exactly one of `class`, `expression`, or `delegate` — `class` is a Java delegate class name, `expression` maps to `operaton:expression`, `delegate` is a friendly alias for `operaton:delegateExpression`), `external` (attribute block: `topic`, delegates to an external worker via `operaton:type="external"` + `operaton:topic`), `script` (a markdown-style fenced body opened by a language tag, e.g. ` ```js … ``` `, mapping to `bpmn:scriptTask`; supported tags are `javascript`/`js`, `groovy`, `python`/`py`, `ruby`/`rb`, and `feel`), `if`/`else if`/`else`, `while (cond) { }`, `do { } while (cond)`, `parallel { { } { } }`, `goto <id>`, `subprocess <id> "<label>"? { }` (a nested flow container mapping to `bpmn:subProcess`; its body is a plain statement block using the same construct vocabulary as a process body — including a nested `subprocess` — and renders expanded, with its children inside the parent's bounds, in BPMN viewers), `call <id> "<label>"? { }` (starts another process — read like a function call at the process boundary: `process = "<id>"` names the called process, an `in` mapping is an argument passed into it and an `out` mapping is a return value read back from it, `in *`/`out *` copies every variable instead of naming one, `version = <number>` pins a specific deployed version of the called process while `binding = latest`/`binding = deployment` selects the other two version-resolution modes, and `businessKey = "<value>"` sets the started process instance's business key; a `local` mapping — e.g. `in local x = y` — reads from / writes to variables that belong to the call step itself rather than to the whole process, which is rarely needed when authoring and is kept chiefly so diagrams that use step-local variables import faithfully). Every targetable statement carries an explicit `id` (e.g. `user ReviewInvoice`), including `subprocess` and `call` — a sequence flow (or `goto`) may target a sub-process or a call in the same container by its id, the same way it targets any other step. The human-readable BPMN `name` is derived from the id when no label is given (`ReviewInvoice` → "Review Invoice"); an optional quoted label after the id overrides it (e.g. `user ReviewInvoice "Review invoice"`).

The event layer reads like try/catch. An `on error`/`on escalation` handler is a catch block, and it sits at the end of the process or subprocess body it guards, the same way a catch block sits after the try body it belongs to (`on error "PAYMENT_FAILED" { … }`). A parenthesized list right after the code is the caught value — the `e` in `catch (Exception e)` — pairing a binding field (`code`, and for `error` also `message`) with the process variable it fills, e.g. `on error "PAYMENT_FAILED" (code c, message m) { … }`; omitting the code (`on error { }`) catches any error of that kind, the catch-all. An `escalation` handler additionally accepts `alongside`, which marks it non-interrupting: the guarded scope's main flow keeps running alongside the handler instead of being interrupted by it (an `error` handler always interrupts, since BPMN has no non-interrupting error). Two verbs fire an event, and the one distinction between them is uniform across every event kind the DSL knows: `throw error "CODE"` and `throw escalation "CODE"` end the current path right there, exactly like `throw` in any programming language, while `emit escalation "CODE"` fires the event and keeps going. A process-header declaration, `error "CODE" message "text"`, records the message text a thrown error of that code carries at runtime — needed only for a code whose throw/catch sites should carry that text, written alongside the `var` declarations at the top of the body.

Only `on`, `throw`, `emit`, and `alongside` are reserved keywords for the event layer. `error`/`escalation` (the trigger kinds) and `code`/`message` (the binding and declaration field names) are deliberately ordinary identifiers, not keywords — `var message: string` and a task named `error` parse exactly like any other identifier — because they are also among the most common variable names a Java-background author reaches for. The editor still highlights and completes them, just only in the positions where they carry event meaning (the first word of `on error`, a binding field, the declaration), never elsewhere.

Condition expressions are parsed as a real AST (JUEL native subset): integer/decimal/string/boolean/null literals, variable references with dot-property and index accessors, unary `!`/`-`, binary arithmetic and comparison operators, logical `&&`/`||`, ternary `? :`, and parentheses. Expressions outside this subset use the quoted raw fallback `"${…}"`.

## Validator checks

The structural validator in `src/bpmn-script-validator.ts` reports:

- **Warning** — an undeclared variable reference (a `VarRef` whose root identifier has no matching `var` declaration in the enclosing process).
- **Error** — a declared-variable type mismatch (e.g. a `string` variable used under a numeric comparison operator).
- **Error** — a duplicate attribute key in an attribute block (e.g. two `assignee =` entries on one `user` task).
- **Error** — a `service` task with zero, or more than one distinct, binding attribute (`class`, `expression`, `delegate`); exactly one is required.
- **Error** — an `external` task with no `topic` attribute.
- **Error** — a `script` task whose fence language tag is not one of the supported aliases, or whose body is empty.
- **Warning** — a process body with no executable statements.
- **Warning** — an empty `subprocess` body (zero statements) — the same empty-body warning a process gets.
- **Error** — a `process` name reused by a later `process` in the same file.
- **Error** — a `var` name declared twice in one process.
- **Error** — a second `label = "…"` declaration in one process.
- **Error** — a step name (`start`/`end`/`user`/`service`/`external`/`script`/`subprocess`) reused by another step anywhere in the same process — even across a sub-process boundary — which would make `goto` ambiguous; BPMN ids must be document-unique, so this stays a whole-process check regardless of the container-scoped `goto` rule below.
- **Warning** — an empty `if`/`else if`/`else` branch, `while`/`do … while` body, or `parallel` branch (zero statements — syntactically legal, almost always a mistake).
- **Error** — a `goto` that reaches into a `parallel` branch from outside that branch's subtree; a branch's steps only run when the whole `parallel` statement is reached.
- **Error** — an explicit `start` anywhere other than the first statement of its process, subprocess, or event-handler body: later in the same container's statement list, or nested inside an `if`/`while`/`do … while`/`parallel` block at any position, is rejected (a start event cannot have incoming flows). A `start` opening a `subprocess` or `on` handler body is valid in the same way one opening a `process` body is; inside a handler body, though, the explicit `start` carries the caught event and rejects a `form` block (the trigger start has no form semantics).
- **Error** — a `call` with no `process` attribute, or an empty `process = ""`.
- **Error** — a `call`'s `binding` value that is neither `latest` nor `deployment` (with a dedicated message for the common `binding = version` slip, pointing at `version = <number>` instead), or a `call` declaring both `binding` and `version` (they are mutually exclusive version-resolution discriminators).
- **Error** — two `call` mappings naming the same target in the same direction (`in`/`out` are independent), including a second bare `in *`/`out *` in one direction; a `*` alongside a named mapping in the same direction is legal.
- **Error** — an `on`/`throw`/`emit` trigger word other than `error`/`escalation`, a binding `field` other than `code`/`message`, or an `error … message` declaration whose leading word or field name is wrong — each names the valid word(s) instead of a bare "unknown identifier" complaint; `emit error "…"` is rejected on its own terms, since an error always ends its path, pointing to `throw error` instead.
- **Error** — an `on` handler that does not sit directly in a process, subprocess, or another handler's body — a handler guards a whole container, not a single `if`/`while`/`parallel` branch.
- **Error** — an `on` handler followed by anything other than another handler in the same body — handlers read like catch blocks and belong at the end.
- **Error** — `on error … alongside` — an error always interrupts the scope it guards; `alongside` only makes sense for `escalation`.
- **Error** — an empty code string on `on`/`throw`/`emit` (to catch or throw *any* error/escalation, omit the code entirely rather than writing `""`).
- **Error** — two handlers in the same body catching the same trigger and code (or both catch-all) — the catch would be ambiguous; a coded handler and a catch-all for the same trigger may coexist.
- **Error** — a duplicate binding field in one handler's parameter list, a `message` binding on an `escalation` handler (escalations carry no message text), or a binding variable already declared elsewhere with a non-`string` type.
- **Error** — a duplicate `error "CODE" message "…"` declaration for the same code, or one with an empty code or message string.
- **Warning** — an empty `on` handler body (zero statements) — the same empty-body warning a process or subprocess gets.

`goto` targets are resolved by a custom `ScopeProvider` (`src/bpmn-script-scope-provider.ts`) that limits a `goto` to the steps of its own nearest enclosing *container* — the `process`, the `subprocess`, or the `on` handler body it directly sits in — at any block-nesting depth within that container. A `goto` can therefore never resolve into another process's steps, into a sibling or nested `subprocess`'s interior, into or out of an `on` handler's body, or out to the parent container from inside one of these, even when both are declared in the same file; a `subprocess` statement is itself a valid `goto` target, resolved from its own container, the same way any other step is (an `on` handler has no name of its own, so it is never itself a target). This mirrors BPMN's rule that a sequence flow cannot cross a sub-process boundary — an event handler being a kind of sub-process, the same rule applies to it: a handler's steps only run once its event fires, so a `goto` cannot pretend otherwise from outside, and the handler's own steps cannot escape back out either. A cross-boundary `goto` therefore fails to resolve — and a custom `Linker` (`src/bpmn-script-linker.ts`) replaces Langium's generic "could not resolve reference" message with a boundary explanation naming the sub-process or handler the target lives inside or outside of (e.g. `'X' is inside subprocess 'Y'; a goto cannot cross a sub-process boundary.`, or, across a handler boundary, naming it by its header since it has no name of its own — `'X' is inside the 'on error "PAYMENT_FAILED"' handler; a goto cannot cross an event handler boundary: an event handler's steps run only when its event fires.`) whenever the referenced name exists elsewhere in the process; the generic message is left unchanged when the name exists nowhere at all. Using a reserved keyword where an identifier is expected (e.g. `if (date > deadline)`) is caught by a custom `ParserErrorMessageProvider` (`src/bpmn-script-parser-error-message-provider.ts`), which names the offending word and points to the quoted `"${…}"` raw-expression fallback instead of surfacing Chevrotain's raw token-mismatch message.

## Build and test

```bash
# From repo root
npm run build --workspace packages/language
npm test --workspace packages/language

# From this directory
npm run build    # runs langium generate + tsc
npm test
```

The `langium:generate` and `langium:watch` scripts are also available for IDE-only use (the VS Code launch configuration relies on them).

## Source layout

| Path                                               | Purpose                                                                                                                 |
| -------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| `src/bpmn-script.langium`                          | Grammar definition (structured statement syntax)                                                                        |
| `src/bpmn-script-validator.ts`                     | AST-level validator: undeclared variables, type mismatches, duplicate attrs, empty body, whole-process integrity checks |
| `src/bpmn-script-scope-provider.ts`                | `ScopeProvider` override: resolves `goto` targets within the enclosing process/subprocess/handler container only        |
| `src/bpmn-script-linker.ts`                        | `Linker` override: replaces the unresolved-`goto`-target message with a sub-process or event-handler boundary explanation |
| `src/bpmn-script-parser-error-message-provider.ts` | `ParserErrorMessageProvider` override: reserved-word-as-identifier guidance                                             |
| `src/bpmn-script-completion.ts`                    | `CompletionProvider` override: snippet completions for structural keywords (`process`, `user`, `if`, …)                 |
| `src/bpmn-script-module.ts`                        | Langium dependency injection wiring                                                                                     |
| `src/expression-render.ts`                         | `renderExpression(astNode): string` — serializes a parsed expression AST to `${…}` text                                 |
| `src/variable-symbol-provider.ts`                  | `VariableSymbolProvider` service — collects `var` declarations into a `VariableTable`                                   |
| `src/index.ts`                                     | Package re-exports                                                                                                      |
| `src/generated/`                                   | Langium-generated files (git-ignored, rebuilt by `npm run build`)                                                       |
| `syntaxes/`                                        | TextMate grammar (git-ignored, generated by `langium generate`)                                                         |
| `test/parsing.test.ts`                             | Grammar/parsing suite (canonical fixture, control-flow constructs, lexer edge cases)                                    |
| `test/validating.test.ts`                          | Validator suite (undeclared vars, type mismatches, duplicate attrs, whole-process integrity checks)                     |
| `test/scoping.test.ts`                             | `goto` container-scoping suite (process, subprocess, and event handler), the boundary diagnostics, and reserved-word parse-error guidance |
| `test/completion.test.ts`                          | Snippet-completion suite                                                                                                |
| `test/variables.test.ts`                           | `VariableSymbolProvider` suite                                                                                          |
